{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks",
    "buildCommand": "cd bloomie-vite && npm install && npm run build",
    "startCommand": "node server.js"
  }
}
